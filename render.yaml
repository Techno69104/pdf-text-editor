services:
  - type: web
    name: pdf-text-editor
    runtime: image
    buildCommand: ./render-build.sh
    startCommand: cd backend && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
