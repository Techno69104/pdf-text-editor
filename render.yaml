services:
  - type: web
    name: pdf-text-editor
    runtime: image
    buildCommand: ./render-build.sh
    startCommand: gunicorn backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
